/**
 * @file
 * Attaches the behaviors for the boardgames theme for mobile.
 */

(function ($, Drupal, drupalSettings) {
	$('#work-js').click(function (){
		alert("JS file work");
	});
})(jQuery, Drupal, drupalSettings);